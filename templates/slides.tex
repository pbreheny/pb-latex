$passoptions.latex()$
\documentclass[table$if(handout)$,handout$endif$]{beamer}
\input{breheny-notes}

$common.latex()$
$after-header-includes.latex()$

$if(graphicspath)$
\graphicspath{{$graphicspath$}}
$endif$

$for(header)$
$header$
$endfor$

% I don't like Quarto's default spacing
\setlength{\parskip}{0.5ex}

\title$if(short-title)$[$short-title$]$endif${$title$}

$if(author)$
\author{$for(author)$$author$$sep$ \and $endfor$}
$endif$
$if(affiliation)$
\newcommand{\affiliation}{$affiliation$}
$endif$

$if(affiliation)$
\newcommand{\affiliation}{$affiliation$}
$endif$

$if(institute)$
\institute{$for(institute)$ $institute$ $sep$ \\ $endfor$}
$endif$

$if(notes)$
\def\footera{$coursename$}
\def\footerb{$coursenumber$}
\def\footerc{$author$}
$else$
\def\footera{$author$}
\def\footerb{$venue$}
\def\footerc{$short$}
$endif$

\date{$date$}

$if(tikz)$
\usepackage{tikz}
\usetikzlibrary{backgrounds,shapes,tikzmark}
$endif$

\begin{document}

$if(title)$
\begin{frame}
  \titlepage
  $if(logo)$
  \UIlogo
  $endif$  
  $if(joint)$
  \begin{center}
    \begin{small}
      Joint work with \\
      $joint$
    \end{small}
  \end{center}
  $endif$
\end{frame}
$endif$

$body$

\end{document}
