$passoptions.latex()$
\documentclass[table$if(handout)$,handout$endif$]{beamer}
\input{breheny-notes}

$common.latex()$
$after-header-includes.latex()$

$for(header)$
$header$
$endfor$

% Make code smaller
\AtBeginDocument{%
  \let\oldttfamily\ttfamily
  \renewcommand{\ttfamily}{\oldttfamily\small}
}

\title$if(short-title)$[$short-title$]$endif${$title$}

$if(author)$
\author{$for(author)$$author$$sep$ \and $endfor$}
$endif$
$if(affiliation)$
\newcommand{\affiliation}{$affiliation$}
$endif$
$if(institute)$
\institute{$institute$}
$endif$

\date{$date$}

$if(tikz)$
\usepackage{tikz}
\usetikzlibrary{backgrounds,shapes,tikzmark}
$endif$

\begin{document}

$if(title)$
\begin{frame}
  \titlepage
  $if(institute)$
  \UIlogo
  $endif$  
  $if(joint)$
  \begin{center}
  \begin{small}
  Joint work with \\
  $joint$
  \end{small}
  \end{center}
  $endif$
\end{frame}
$endif$

$body$

\end{document}
